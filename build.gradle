plugins {
	id 'java'
	id 'maven-publish'
	id 'jacoco'
    id "org.sonarqube" version "3.5.0.2730"
}

repositories {
    mavenCentral()
}

ext{
	vertxVersion = '4.4.1'
	jupiterVersion = '5.9.1'
	jupiterLauncherVersion = '1.9.1'
	slfApiVersion = '2.0.5'
	logbackVersion = '1.4.5'
}

dependencies {

	implementation "io.vertx:vertx-web:${vertxVersion}"
	
	testImplementation "io.vertx:vertx-config-yaml:${vertxVersion}"
	testImplementation "io.vertx:vertx-junit5:${vertxVersion}"
	testImplementation "io.vertx:vertx-web-client:${vertxVersion}"
	
	testImplementation "org.junit.jupiter:junit-jupiter-api:${jupiterVersion}"
    testRuntimeOnly "org.junit.jupiter:junit-jupiter-engine:${jupiterVersion}"
    testImplementation "org.junit.platform:junit-platform-launcher:${jupiterLauncherVersion}"
	
	testRuntimeOnly "org.slf4j:slf4j-api:${slfApiVersion}"
	testRuntimeOnly "ch.qos.logback:logback-classic:${logbackVersion}"
}

test {
	environment "envVar1", "envVal1"
    useJUnitPlatform()
    finalizedBy jacocoTestReport
}

sourceCompatibility='1.8'
targetCompatibility='1.8'

jar.archiveFileName = "vertx-web-accesslog-1.6.0_RC1.jar"

java {
    withSourcesJar()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            
            groupId 'com.mdac'
            artifactId 'vertx-web-accesslog'
            version '1.6.0_RC1'
            
            from components.java

        }
    }
}

sonarqube {
    properties {
    	property "sonar.sources", "src/main"
    	property "sonar.tests", "src/test"
        property "sonar.test.exclusions", "**/src/test/**"
    }
}


jacocoTestReport {
	reports {
		xml.required = true
		html.required = true
	}
}

wrapper() {
  gradleVersion = '8.0.2'
}

